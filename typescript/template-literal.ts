(function () {
        const nombre = "Israel";
        const apellido = "Cetina";
        const edad = 28;

        //concatenando
        //const salida = nombre + " " + apellido + " ( " +  edad + " )";
        const salida = `${nombre} ${apellido} (${edad})`; //es mucho más corto y eficiente que concatenar

    }
)();